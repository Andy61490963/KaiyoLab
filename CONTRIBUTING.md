# 參與 KaiyoLab 開發

歡迎協助改善使用體驗、功能與文件。請先閱讀 README，理解單站單管理者的定位。

## 開始開發

1. Fork 專案並建立功能分支。
2. 使用 Node.js 24、PostgreSQL 18，執行 `npm ci`。
3. 依 README 設定本機開發環境，使用獨立資料庫，勿連接正式站。
4. 執行 migration 後，以 `npm run dev` 啟動。

新增或修改介面、文件、錯誤訊息與 commit 訊息請使用**繁體中文**。程式識別字及標準 API 名稱使用慣用英文。

## 變更原則

- 公開讀取只可使用已發布的快照；不能直接公開編輯中的草稿。
- 後台頁面與管理 API 都要驗證 Session；寫入操作要驗證來源與輸入。
- 預覽與公開文章共用 Markdown 流程，不引入可執行的 MDX。
- Schema 變更以新增版本化 migration 交付，已發布的 migration 不回寫。
- 圖片與站點設定需要考慮備份、還原、相對路徑及反向代理環境。
- 介面保持鍵盤可操作，考慮明暗模式、手機尺寸與減少動態偏好。
- 不提交 `.env`、上傳檔案、備份、個人資料或任何密鑰。

## 提交前驗證

```bash
npm run check
npm test
npm run test:integration
npm run build
npm run test:e2e
```

依變更範圍完成合適的測試。登入、初始化、發布隔離與資料保存等核心行為應有有效的測試情境。調整 Docker、migration 或檔案儲存時，實際使用乾淨 volumes 啟動並驗證備份還原；不要以配置檔語法檢查代替。

PR 請說明改變了什麼、使用者能看到的影響、完成的驗證與已知限制。畫面變更附上實際截圖；未執行的測試請明確註明。

若回報涉及可被利用的安全問題，請勿在公開 issue 貼上可存取私人資料的步驟、密鑰或真實使用者資訊。先使用 GitHub 私人漏洞回報功能（專案啟用時）聯繫維護者。

提交貢獻代表同意以專案 MIT 授權散布你的貢獻，並保留外部素材及依賴原有的授權要求。
