---
import PublicLayout from '../layouts/PublicLayout.astro';
import EntryCard from '../components/public/EntryCard.astro';
import ProjectCard from '../components/public/ProjectCard.astro';
import Icon from '../components/public/Icon.astro';
import { getSettings, listPublished, listTaxonomies } from '../lib/content';
const [settings, recent, featured, projects, taxonomy] = await Promise.all([
  getSettings(),
  listPublished({ kind: 'article', pageSize: 5 }),
  listPublished({ kind: 'article', featured: true, pageSize: 3 }),
  listPublished({ kind: 'project', featured: true, pageSize: 3 }),
  listTaxonomies(),
]);
const recentIds = new Set(recent.items.map((entry) => entry.id));
const additionalFeatured = featured.items.filter((entry) => !recentIds.has(entry.id));
const authorName = settings.authorName || settings.siteName;
---

<PublicLayout settings={settings}>
  <div class="page-shell home-content">
    <header class="home-introduction">
      <h1>嗨，我是 {authorName}</h1>
      <p class="home-tagline">{settings.tagline || '寫程式、做作品，也記錄學習過程中的發現'}</p>
      <p>
        {settings.description ||
          '歡迎來到我的個人網站，這裡分享技術筆記、開發經驗，以及把想法變成作品的過程'}
      </p>
      <a class="home-inline-link" href="/about">
        更多關於我 <span aria-hidden="true">→</span>
      </a>
      {settings.heroImage && settings.heroImage !== '/images/kaiyo-hero.png' && (
        <img
          class="home-custom-image"
          src={settings.heroImage}
          alt={`${settings.siteName} 的首頁圖片`}
          width="960"
          height="400"
          fetchpriority="high"
        />
      )}
    </header>

    <section class="home-section" aria-labelledby="recent-articles-title">
      <div class="home-section-heading">
        <h2 id="recent-articles-title">最新文章</h2>
        <a href="/articles">
          所有文章 <span aria-hidden="true">→</span>
        </a>
      </div>
      {recent.items.length ? (
        <div class="home-article-list">
          {recent.items.map((entry) => (
            <EntryCard entry={entry} />
          ))}
        </div>
      ) : (
        <p class="home-empty">目前沒有已發布的文章</p>
      )}
    </section>

    {additionalFeatured.length > 0 && (
      <section class="home-section" aria-labelledby="featured-articles-title">
        <div class="home-section-heading">
          <h2 id="featured-articles-title">精選文章</h2>
        </div>
        <div class="home-article-list">
          {additionalFeatured.map((entry) => (
            <EntryCard entry={entry} />
          ))}
        </div>
      </section>
    )}

    <section class="home-section" aria-labelledby="featured-projects-title">
      <div class="home-section-heading">
        <h2 id="featured-projects-title">精選作品</h2>
        <a href="/projects">
          所有作品 <span aria-hidden="true">→</span>
        </a>
      </div>
      {projects.items.length ? (
        <div class="home-project-list">
          {projects.items.map((entry) => (
            <ProjectCard entry={entry} headingLevel="h3" />
          ))}
        </div>
      ) : (
        <p class="home-empty">目前沒有精選作品，可到作品頁查看全部內容</p>
      )}
    </section>

    {taxonomy.categories.length > 0 && (
      <section class="home-section home-topics" aria-labelledby="topics-title">
        <h2 id="topics-title">依主題閱讀</h2>
        <ul>
          {taxonomy.categories.map((category) => (
            <li>
              <a href={`/articles?category=${encodeURIComponent(category.name)}`}>
                {category.name}
              </a>
            </li>
          ))}
        </ul>
      </section>
    )}

    <p class="home-subscribe">
      <Icon name="rss" size={18} />
      <span>
        <a href="/rss.xml">透過 RSS 訂閱最新文章</a>
      </span>
    </p>
  </div>
</PublicLayout>

<style>
  .home-content {
    padding-block: 64px 48px;
  }
  .home-introduction {
    max-width: 820px;
    margin-bottom: 52px;
  }
  .home-introduction h1 {
    margin-bottom: 24px;
    font-size: clamp(30px, 3.2vw, 42px);
    font-weight: 700;
    line-height: 1.3;
    letter-spacing: -0.035em;
  }
  .home-introduction p {
    margin-bottom: 16px;
    font-size: 18px;
    line-height: 1.9;
  }
  .home-introduction .home-tagline {
    font-size: 21px;
    line-height: 1.75;
  }
  .home-inline-link,
  .home-section-heading a,
  .home-topics a,
  .home-subscribe a {
    color: var(--accent);
    font-weight: 600;
    text-underline-offset: 4px;
  }
  .home-inline-link:hover,
  .home-section-heading a:hover,
  .home-topics a:hover,
  .home-subscribe a:hover {
    text-decoration: underline;
  }
  .home-custom-image {
    width: 100%;
    max-height: 300px;
    margin-top: 28px;
    border-radius: 6px;
    object-fit: cover;
  }
  .home-section {
    margin-block: 42px;
  }
  .home-section-heading {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 18px;
  }
  .home-section h2 {
    font-size: 25px;
    font-weight: 700;
    line-height: 1.5;
    letter-spacing: -0.025em;
  }
  .home-section-heading a {
    flex-shrink: 0;
    font-size: 15px;
  }
  .home-article-list {
    border-top: 1px solid var(--border);
  }
  .home-project-list {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 24px;
  }
  .home-empty {
    padding: 20px 0;
    border-top: 1px solid var(--border);
    color: var(--muted);
    font-size: 16px;
  }
  .home-topics ul {
    display: flex;
    flex-wrap: wrap;
    gap: 10px 22px;
    padding: 0;
    margin: 18px 0 0;
    list-style: none;
    font-size: 16px;
  }
  .home-subscribe {
    display: flex;
    align-items: center;
    gap: 12px;
    padding-top: 24px;
    margin-top: 48px;
    border-top: 1px solid var(--border);
    color: var(--muted);
    font-size: 15px;
    line-height: 1.8;
  }
  @media (max-width: 1199px) {
    .home-project-list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  @media (max-width: 600px) {
    .home-project-list {
      grid-template-columns: minmax(0, 1fr);
    }
    .home-content {
      padding-block: 32px;
    }
    .home-introduction {
      margin-bottom: 36px;
    }
    .home-introduction p {
      font-size: 16px;
    }
    .home-introduction .home-tagline {
      font-size: 18px;
    }
    .home-section {
      margin-block: 32px;
    }
    .home-section h2 {
      font-size: 23px;
    }
    .home-section-heading a {
      font-size: 14px;
    }
  }
</style>
